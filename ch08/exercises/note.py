import pygame
import point
##controller=like a model label for how class is used

#model= data
#view=display(pygame)
#controller=logic (director)
def mainloop():
    display=pygame.display.set_mode()
    points=[]
    #mainloop
    while True:
        #eventloop
        for event


class Controller:
    def __init__(self)
        
def mainloop(self):
    while TRue:
        for evenet in pygmae.event.get()
            if event.type==pygame.Quit:
                pygame.quit()
                exit()
            elif event.type ==pygame.MOUSEBUTTONUP:
                self.drawing= False
            elif event.type ==pygame.MOUSEBUTTONUP:
                self.drawing=False
            selif event.type ==pygame.MOUSEMOTION
class Point(pygame.sprite.Sprite):
    def __init__(self):
